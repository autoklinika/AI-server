# Walidacja profilu wentylacji v12.2

Data: 2026-08-19

Profil: `ventilation-v12.2-adverse-trend-gate`

Model: `qwen3.6:35b`

Tryb: `think=false`

## Cel

v12.2 kończy serię zmian rozpoczętą po wykryciu, że swobodny tekst modelu może zawierać błędne liczby, pomylić kanały `supply/extract`, dopisać nieistniejącą przyczynowość lub nieprawidłowo zinterpretować pola deterministyczne.

Architektura v12.x rozdziela odpowiedzialności:

- Python wylicza i zachowuje fakty deterministyczne,
- Python rozstrzyga aktywne alarmy, jakość danych, degradację techniczną, końcową hierarchię statusu i operator-facing rendering,
- Qwen wykonuje wyłącznie ograniczoną interpretację trendów środowiskowych i wybiera identyfikatory faktów, które uzasadniają `environmental_attention`,
- Qwen nie generuje operator-facing liczb ani swobodnych zaleceń technicznych.

## Fast gate v12.2

Testy deterministyczne profilu: PASS.

Fast gate modelu:

- `pm_rise` -> `environmental_attention=true`, final `attention`: PASS,
- `recent_clean_reference` -> `environmental_attention=false`, final `no_anomaly_detected`: PASS.

Wynik: **2/2 PASS** przy tylko dwóch wywołaniach Ollamy.

## Walidacja na sześciu archiwalnych rzeczywistych oknach

Walidacja była read-only i nie zapisywała rekordów do `ventilation_analysis_runs`.

1. `voc_bilateral_event`
   - wynik: `attention`
   - powód: `environmental_change`
   - poprawnie wskazane wzrosty VOC Index w obu węzłach.

2. `pm_rise`
   - wynik: `attention`
   - powód: `environmental_change`
   - poprawnie wskazane wzrosty frakcji PM.

3. `pm_fall`
   - wynik: `attention`
   - powód: `environmental_change`
   - model wskazał wzrost VOC Index, mimo równoczesnego spadku PM; nie tworzono przyczynowości.

4. `sensor_bus_alarm_window`
   - wynik: `anomaly`
   - powody obejmują `technical_alarm`, `technical_degradation`, `data_quality_issue`, `environmental_change`
   - status techniczny, alarmy i braki danych zostały rozstrzygnięte deterministycznie przez Python.

5. `voc_complex_with_aero_alarm`
   - wynik: `anomaly`
   - powody obejmują `technical_alarm`, `environmental_change`
   - `AERO_BUS_UNAVAILABLE` pozostał dokładnym kodem alarmu bez mapowania na SENSOR BUS.

6. `recent_clean_reference`
   - wynik: `no_anomaly_detected`
   - powód: `no_significant_issue`
   - spadki PM/VOC nie są już traktowane jako `attention` tylko dlatego, że trend jest wyraźny.

## Wniosek

v12.2 zalicza oczekiwany zestaw statusów dla wszystkich sześciu rzeczywistych okien:

- attention
- attention
- attention
- anomaly
- anomaly
- no_anomaly_detected

Najważniejsze klasy błędów z v11.x zostały wyeliminowane przez zmianę architektury, a nie przez dalsze rozbudowywanie swobodnego promptu.

Po tej walidacji profil v12.2 został przygotowany do aktywacji w właściwym serwisie analizy. Produkcyjny serwis powinien używać wersji promptu `ventilation-v12.2-adverse-trend-gate`, struktury `EnvironmentalDecisionV122` oraz deterministycznego renderera v12.x.
